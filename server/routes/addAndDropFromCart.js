const express = require('express')
const router = express.Router()
router.get('/',(req, res ) =>{

    const username = null;
    const product = null;
    const status = null;
    res.send('Welcome to add and drop cart page')

    
})

module.exports = router